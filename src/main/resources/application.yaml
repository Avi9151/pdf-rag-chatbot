spring:
  application:
    name: pdf-rag-chatbot

  ai:
    ollama:
      base-url: http://localhost:11434

      chat:
        model: gemma3:1b
        options:
          temperature: 0.3

      embedding:
        model: nomic-embed-text

server:
  port: 8080